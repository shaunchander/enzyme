import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'app.getenzyme.app',
	appName: 'Enzyme',
	webDir: 'dist',
	bundledWebRuntime: false
};

export default config;
